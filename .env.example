# Cursor Agent CLI — qualitative & supply scoring (`cursor-agent --print`)
# Install and auth: use Cursor CLI, then run `cursor-agent login`
#
# CURSOR_AGENT_BIN=cursor-agent
# CURSOR_AGENT_MODEL=auto
# CURSOR_AGENT_RUN_TIMEOUT=300
# CURSOR_AGENT_ADOPTION_TIMEOUT=300
# CURSOR_AGENT_SUPPLY_TIMEOUT=60

# FRED API key (optional — improves GLI filter when set)
# FRED_API_KEY=xxx

# CoinGecko API key (optional — free tier works but has rate limits)
# COINGECKO_API_KEY=CG-xxxxx
